{
  "name": "furniture-ai-site",
  "version": "1.0.0",
  "description": "AI-powered furniture interior visualization platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm ci --prefer-offline --no-audit && npm run build",
    "start": "cd backend && npm start",
    "start:full": "cd backend && npm run start:full",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
