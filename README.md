# 🛋️ FurniAI - Мебельный AI-Сервис

Современная веб-платформа для визуализации мебели в интерьере с использованием **Google Gemini API**.

## 🎯 Основные возможности

✅ **Загрузка фотографий интерьера** - Загрузьте фото своей комнаты  
✅ **AI-анализ интерьера** - Gemini анализирует стиль, цвета, освещение  
✅ **AI-консультант** - Диалоговый режим для советов и рекомендаций  
✅ **Генерация мебели** - AI предлагает подходящую мебель  
✅ **Кастомизация** - Меняйте размеры, цвет, материал мебели  
✅ **Галерея интерьеров** - Публикуйте и делитесь своими проектами  
✅ **Премиальный дизайн** - Темно-красная цветовая схема, минимализм  

## 🏗️ Технологический стек

### Frontend
- **Next.js 14** - React фреймворк с SSR
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **Zustand** - Управление состоянием
- **Axios** - HTTP клиент
- **React Hot Toast** - Уведомления

### Backend
- **Node.js + Express** - REST API сервер
- **Google Generative AI SDK** - Интеграция с Gemini
- **Multer** - Загрузка файлов
- **CORS** - Кроссдоменные запросы
- **UUID** - Уникальные идентификаторы

### API
- **Google Gemini 2.0 Flash** - Единственное используемое AI решение

## 📁 Структура проекта

```
furniture-ai-site/
├── frontend/              # Next.js приложение
│   ├── src/
│   │   ├── app/          # App Router (главная, страницы)
│   │   ├── components/   # React компоненты
│   │   ├── lib/          # API клиент, утилиты
│   │   └── store/        # Zustand хранилище
│   ├── public/           # Статические файлы
│   ├── tailwind.config.js
│   ├── tsconfig.json
│   └── package.json
│
├── backend/              # Express сервер
│   ├── src/
│   │   ├── index.js      # Главный файл сервера
│   │   ├── routes/       # API маршруты
│   │   │   ├── aiRoutes.js
│   │   │   ├── interiorRoutes.js
│   │   │   └── uploadRoutes.js
│   │   ├── services/
│   │   │   └── geminiService.js
│   │   └── middleware/
│   ├── data/             # JSON хранилище
│   ├── .env
│   └── package.json
│
└── package.json          # Root workspace
```

## 🚀 Быстрый старт

### Требования
- **Node.js 18+**
- **Google Gemini API ключ** (бесплатный)

### 1. Установка зависимостей

```bash
cd furniture-ai-site
npm install
```

### 2. Конфигурация

**Backend .env** (`backend/.env`):
```env
GEMINI_API_KEY=your_api_key_here
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

**Frontend .env.local** (`frontend/.env.local`):
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### 3. Получить API ключ

1. Перейти на https://ai.google.dev/
2. Нажать "Get API Key"
3. Создать новый проект (если нужен)
4. Скопировать API ключ
5. Вставить в `backend/.env`

### 4. Запуск разработки

```bash
npm run dev
```

Откроется:
- Frontend: http://localhost:3000
- Backend: http://localhost:3001

## 📡 API Endpoints

### AI Endpoints

#### POST `/api/ai/analyze`
Анализ интерьера по изображению.

**Request:**
```json
{
  "imageBase64": "base64_encoded_image"
}
```

**Response:**
```json
{
  "roomType": "Living Room",
  "style": "Modern",
  "colors": ["white", "gray", "wood"],
  "lighting": "Natural light",
  "furnishing": "Well-furnished",
  "recommendations": ["Add more plants", "Warm lighting"],
  "furnitureNeeds": ["Coffee table", "Bookshelf"]
}
```

#### POST `/api/ai/chat`
Диалог с AI консультантом.

**Request:**
```json
{
  "messages": [
    {"role": "user", "content": "Какую мебель добавить?"},
    {"role": "assistant", "content": "Рекомендую..."}
  ],
  "interiorContext": {
    "roomType": "Living Room",
    "style": "Modern"
  }
}
```

**Response:**
```json
{
  "message": "Для современного гостиного можно добавить..."
}
```

#### POST `/api/ai/furniture-proposals`
Генерация предложений мебели.

**Request:**
```json
{
  "roomType": "Living Room",
  "style": "Modern",
  "dimensions": {
    "width": 400,
    "depth": 300
  }
}
```

**Response:**
```json
{
  "proposals": [
    {
      "name": "Modern Sofa",
      "description": "3-seater contemporary sofa",
      "style": "Modern",
      "color": "#333333",
      "dimensions": {
        "width": 180,
        "depth": 90,
        "height": 80
      },
      "price_range": "$800-$1200",
      "placement": "Along the main wall"
    }
  ]
}
```

### Interior Endpoints

#### GET `/api/interiors`
Получить все опубликованные интерьеры.

#### POST `/api/interiors`
Создать новый интерьер.

#### PUT `/api/interiors/:id/publish`
Опубликовать интерьер.

#### PUT `/api/interiors/:id/furniture`
Добавить мебель.

#### POST `/api/interiors/:id/view`
Увеличить счётчик просмотров.

#### POST `/api/interiors/:id/like`
Добавить лайк.

### Upload Endpoint

#### POST `/api/upload`
Загрузить изображение.

**Request:** multipart/form-data с полем `file`

**Response:**
```json
{
  "success": true,
  "fileId": "uuid",
  "mimeType": "image/jpeg",
  "size": 1024000,
  "imageBase64": "data:image/jpeg;base64,..."
}
```

## 🎨 Дизайн и цвета

### Цветовая палитра
- **#6B0F1A** - Тёмно-бордовый (primary-dark)
- **#8C1D18** - Глубокий красный (primary-main)
- **#A62E25** - Светлый красный (primary-light)
- **#1A1A1A** - Почти чёрный фон (dark-bg)
- **#FFFFFF** - Белый текст

### Компоненты дизайна
- Мягкие тени (`shadow-soft`, `shadow-premium`)
- Закруглённые углы (16px border-radius)
- Минимальное количество текста
- Большие интерактивные элементы
- Плавные анимации (Framer Motion)

## 📄 Страницы приложения

1. **Home** (`/`) - Главная страница с презентацией
2. **Upload** (`/upload`) - Загрузка и редактирование интерьера
3. **AI Showroom** (`/showroom`) - Каталог мебели с фильтрами
4. **Gallery** (`/gallery`) - Галерея опубликованных интерьеров
5. **Interior Detail** (`/interior/:id`) - Просмотр конкретного интерьера
6. **Profile** (`/profile`) - Профиль пользователя

## 🔄 Workflow

1. Пользователь загружает фото интерьера
2. Backend отправляет изображение в Gemini API
3. Gemini анализирует интерьер (стиль, цвета, освещение)
4. Результаты отображаются в UI
5. Пользователь может общаться с AI консультантом
6. AI предлагает мебель на основе анализа
7. Пользователь редактирует размеры и параметры мебели
8. Интерьер публикуется в галерею
9. Другие пользователи могут просматривать и добавлять мебель

## 🔐 Security Notes

- Используйте переменные окружения для чувствительных данных
- Валидируйте размеры загружаемых файлов (макс 10MB)
- Добавьте аутентификацию для production версии
- Используйте HTTPS в production

## 📝 Примеры использования

### Анализ интерьера в консоли

```javascript
// frontend/src/lib/api.ts используется везде

import { aiService } from '@/lib/api';

// Анализ
const analysis = await aiService.analyzeInterior(imageBase64);
console.log(analysis.roomType, analysis.style);

// Консультант
const response = await aiService.chat([
  { role: 'user', content: 'Какую мебель добавить?' }
], analysis);

// Мебель
const proposals = await aiService.generateFurnitureProposals(
  'Living Room',
  'Modern',
  { width: 400, depth: 300 }
);
```

## 🐛 Troubleshooting

### Backend не запускается
```bash
# Проверить переменные окружения
cat backend/.env

# Переустановить зависимости
cd backend && rm -rf node_modules && npm install

# Запустить с логированием
NODE_DEBUG=* npm run dev
```

### Frontend не подключается к API
```bash
# Проверить NEXT_PUBLIC_API_URL
echo $NEXT_PUBLIC_API_URL

# Проверить, работает ли backend
curl http://localhost:3001/health
```

### Ошибки Gemini API
- Проверить валидность API ключа
- Убедиться что база64 изображения валидна
- Проверить лимиты запросов на Google AI Studio

## 🚀 Production Deploy

### Backend (Heroku, Railway, Render)
```bash
# Убедитесь в наличии .env
git push heroku main
```

### Frontend (Vercel, Netlify)
```bash
# Vercel автоматически обнаружит Next.js
npm run build
vercel deploy --prod
```

## 📊 Статистика проекта

- **Frontend компоненты:** 7
- **Backend маршруты:** 3 главных модуля
- **API endpoints:** 10+
- **Страницы:** 6
- **Цветовые схемы:** 1 (темно-красная)
- **Зависимости:** 30+ пакетов

## 💡 Возможные улучшения

- [ ] Аутентификация пользователей (Auth0, Firebase)
- [ ] Сохранение интерьеров в базе данных (PostgreSQL)
- [ ] Загрузка изображений в облако (AWS S3, GCS)
- [ ] Улучшенная визуализация AR (Three.js, Babylon.js)
- [ ] Интеграция платежей (Stripe, PayPal)
- [ ] Реал-тайм коллаборация (WebSockets)
- [ ] Рекомендации мебели через ML
- [ ] Экспорт проектов (PDF, 3D модели)

## 📞 Support

Для вопросов и проблем:
1. Проверить документацию Gemini API: https://ai.google.dev/
2. Читать логи backend и frontend консолей
3. Проверить сетевые запросы (DevTools Network tab)

## 📄 License

MIT License - свободное использование в личных и коммерческих проектах

---

**Создано с ❤️ для любителей дизайна интерьера и современных технологий**
