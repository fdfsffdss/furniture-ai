# 🔗 Объединённое приложение (Монолит)

## 📌 Архитектура

Теперь фронтенд и бэкенд работают как один монолит:

```
┌─────────────────────────────────────┐
│         Express Server (Port 3001)  │
│  ┌───────────────────────────────┐  │
│  │  API Routes (/api/*)          │  │
│  ├───────────────────────────────┤  │
│  │  Static Files (Next.js build) │  │
│  │  Serve на / (root)            │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 🚀 Для Development

### Terminal 1: Backend (с автоматической перезагрузкой)
```bash
cd backend
npm run dev
```

### Terminal 2: Frontend (Next.js dev server)
```bash
cd frontend
npm run dev
```

Оба работают параллельно:
- Frontend: `http://localhost:3000` (hot reload)
- Backend: `http://localhost:3001` (API)

## 🏗️ Для Production

### Build и запуск в одной команде:
```bash
# Из папки backend
npm run start:full
```

Это автоматически:
1. ✅ Installует зависимости frontend
2. ✅ Строит Next.js в `.next/` 
3. ✅ Запускает Express сервер
4. ✅ Serve фронтенд на том же порту

## 📦 Production файловая структура

```
backend/
├── src/
│   └── index.js          ← сервит фронтенд
├── .env
├── package.json
└── node_modules/

frontend/
├── src/
├── .next/                ← генерируется при build
│   ├── static/
│   ├── standalone/       ← используется при production
│   └── server/
└── public/
```

## 🌍 Deployment на Railway (всё вместе)

### 1️⃣ Обновить Railway config

**`backend/railway.toml`** уже готов для монолита.

### 2️⃣ В Railway Dashboard:

```
Build Command:    npm run build
Start Command:    npm start
Port:            3000
```

### 3️⃣ Environment Variables:

```
GOOGLE_API_KEY     = sk-...
NODE_ENV           = production
PORT               = 3000
NEXT_PUBLIC_API_URL = https://your-railway-app.railway.app
```

### 4️⃣ Deploy:

```bash
# Просто push в main - Railway автоматически:
git push origin main
```

Railway будет:
1. Скачает код
2. Выполнит `npm run build` → подготовит фронтенд
3. Выполнит `npm start` → запустит Express с фронтендом
4. Всё доступно на одном URL!

## ✅ После Deploy

Просто откройте:
```
https://your-app.railway.app
```

И вы получите:
- ✅ Фронтенд (Next.js)
- ✅ API (Express)
- ✅ Статические файлы
- ✅ Всё на одном сервере

## 💰 Стоимость

**До**: Vercel ($0) + Railway ($5) = **$5/месяц**
**Сейчас**: Railway только ($5) = **$5/месяц**

Сэкономили на одном хостинге, но проще развёртывание!

## 🔄 Git workflow

```bash
# Локальная разработка
cd backend && npm run dev    # Terminal 1
cd frontend && npm run dev   # Terminal 2

# Когда готово к production
git add .
git commit -m "feature: something"
git push origin main

# Railway автоматически:
# 1. Скачает код
# 2. Выполнит npm run build (подготовит фронтенд)
# 3. Выполнит npm start (запустит сервер)
# 4. Ваше приложение live! 🚀
```

## 📝 Что изменилось в коде

### `backend/src/index.js`
- ✅ Добавлены `path` imports
- ✅ Serve Next.js standalone build
- ✅ Serve public files
- ✅ Fallback для SPA (все неизвестные пути → index.html)

### `backend/package.json`
- ✅ `npm run build` теперь строит фронтенд
- ✅ `npm start` запускает Express + фронтенд

### `frontend/next.config.js`
- ✅ Добавлен `output: 'standalone'`
- ✅ Меньше зависимостей в production build

## 🐛 Решение проблем

### "Cannot find module 'path'"
```bash
# Убедитесь что используется ES modules в package.json:
"type": "module"
```

### Frontend выглядит старо (не обновляется)
```bash
# Пересоздайте build
cd frontend
npm run build
cd ../backend
npm run start:full
```

### API возвращает 404
```bash
# Убедитесь что используете правильный URL:
# ✅ /api/ai/analyze
# ❌ http://localhost:3001/api/ai/analyze (не нужен base URL)
```

---

**🎉 Готово! Теперь у вас полностью интегрированное приложение!**
