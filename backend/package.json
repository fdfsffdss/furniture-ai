{
  "name": "designyx-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "cd ../frontend && npm run build",
    "build:frontend": "cd ../frontend && npm run build && npm run build:standalone",
    "start": "node src/index.js",
    "start:full": "npm run build:frontend && npm start"
  },
  "dependencies": {
    "@google/generative-ai": "^0.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
