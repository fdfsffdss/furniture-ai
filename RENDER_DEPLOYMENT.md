# 🚀 Deploy на Render (Бесплатно!)

## ✅ Что вам понадобится

1. GitHub аккаунт (уже есть ✓)
2. Render аккаунт (https://render.com) — бесплатно
3. Google Gemini API ключ

---

## 📋 STEP 1: Подготовить Render config

Создайте файл **`render.yaml`** в **корне проекта** (рядом с backend и frontend папками):

```yaml
# render.yaml
services:
  - type: web
    name: furniture-ai
    runtime: node
    region: oregon
    plan: free
    
    # Root directory где package.json
    rootDir: backend
    
    # Build
    buildCommand: npm install && cd ../frontend && npm install && cd ../backend && npm run build:frontend
    startCommand: node src/index.js
    
    # Port
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: GOOGLE_API_KEY
        sync: false  # добавить вручную в Dashboard
      - key: NEXT_PUBLIC_API_URL
        value: https://furniture-ai.onrender.com  # обновить с вашим URL
```

Добавьте в git:
```bash
git add render.yaml
git commit -m "add: render deployment config"
git push origin main
```

---

## 🎯 STEP 2: Deploy на Render

### 2.1 Создайте аккаунт на Render

1. Перейдите https://render.com
2. **Sign up** → GitHub (используйте свой аккаунт GitHub)
3. Разрешите доступ к репозиториям

### 2.2 Deploy приложения

1. В Render Dashboard → **New +**
2. Выберите **Web Service**
3. Подключите репозиторий:
   - Выберите `fdfsffdss/furniture-ai`
   - Нажмите **Connect**

4. **Settings для Web Service**:
   ```
   Name:                  furniture-ai
   Runtime:               Node
   Region:                Oregon (Free tier)
   Plan:                  Free
   Build Command:         npm install && cd ../frontend && npm install && cd ../backend && npm run build:frontend
   Start Command:         node src/index.js
   ```

5. **Environment Variables** (в Dashboard):
   - Нажмите **Add Environment Variable**
   - Добавьте:
     ```
     GOOGLE_API_KEY = AIza...ваш-настоящий-ключ...
     NODE_ENV = production
     PORT = 3000
     NEXT_PUBLIC_API_URL = https://furniture-ai.onrender.com
     ```
     
     (Замените `AIza...` на ваш ключ из Google AI Studio)

6. Нажмите **Create Web Service**

---

## ⏳ STEP 3: Ждём deployment

Render будет:
1. Скачивает код с GitHub ✓
2. Устанавливает зависимости ✓
3. Строит фронтенд ✓
4. Запускает Express сервер ✓
5. **Доступно на https://furniture-ai.onrender.com** ✓

Процесс займёт **3-5 минут**.

**Logs видны в Dashboard** → вкладка **Logs**

---

## 📱 STEP 4: Проверяем

### Фронтенд загружается?
```
https://furniture-ai.onrender.com
```
Вы должны увидеть ваше приложение ✅

### API работает?
```bash
curl https://furniture-ai.onrender.com/health
```
Ответ:
```json
{
  "status": "ok",
  "timestamp": "2026-01-22T...",
  "environment": "production"
}
```

### Проверить в браузере
- Откройте DevTools (F12)
- Network tab
- Отправьте запрос к AI
- Должен пойти в `/api/ai/...` ✓

---

## 🔄 Git автоматизация

После этого, каждый раз когда вы:
```bash
git push origin main
```

Render **автоматически**:
1. Скачает новый код
2. Пересоберёт приложение
3. Перезагрузит сервер
4. Ваши изменения live! 🚀

**Не нужно ничего делать вручную!**

---

## ⚠️ Важные моменты

### Free tier Render

✅ Что включено:
- 750 часов/месяц работы сервера
- Shared CPU
- 0.5GB RAM
- Полностью бесплатно

❌ Ограничения:
- Сервер **спит после 15 минут неактивности**
- Первый запрос после сна **медленнее** (~30 сек)
- Если просыпается часто — может исчерпать часы

### Что делать с sleep?

**Вариант 1**: Ничего не делать (для хобби-проекта OK)

**Вариант 2**: Купить Paid plan ($7/месяц) — убирает sleep

**Вариант 3**: Использовать UptimeRobot для ping каждые 14 минут:
- https://uptimerobot.com (бесплатно)
- Создайте HTTP(s) monitor
- URL: `https://furniture-ai.onrender.com/health`
- Interval: каждые 14 минут

---

## 🔐 Безопасность

### Gemini API Key
- ❌ Никогда не коммитьте в GitHub
- ✅ Добавляйте ТОЛЬКО в Render Environment Variables
- ✅ Используйте `.env.example` как шаблон

### Logs видят только вы
- Render Dashboard приватный
- Логи не видны никому без доступа

---

## 💰 Стоимость

| Сервис | Free Tier | Цена |
|--------|-----------|------|
| **Render** | 750 ч/месяц | **Бесплатно** |
| **Google Gemini** | 60 req/min | Бесплатно |
| **GitHub** | Неограниченные repo | Бесплатно |
| **Итого** | | **$0/месяц** 🎉 |

Полностью бесплатный production!

---

## 📊 Мониторинг

### Render Dashboard показывает:
- **Logs** - что происходит на сервере
- **Metrics** - CPU, Memory, Network
- **Events** - Deploy history
- **Health** - статус сервера

### Проверить статус:
```bash
# Здоровье сервера
curl https://furniture-ai.onrender.com/health

# Список endpoints
curl https://furniture-ai.onrender.com/
```

---

## 🚨 Если что-то не работает

### Ошибка 502 Bad Gateway
```
Render Dashboard → Logs
Проверьте что выводит сервер
```

### Сервер не запускается
```bash
# Проверить build error
1. Render Dashboard → Logs
2. Найти ошибку в Build section
3. Исправить локально
4. git push → автоматический deploy
```

### API возвращает 404
```bash
# Проверить что используется правильный базовый URL
# ✅ fetch(`${NEXT_PUBLIC_API_URL}/api/ai/...`)
# ❌ fetch('/api/ai/...') - неправильно для production
```

### Фронтенд выглядит старо
```bash
# Очистить кэш браузера
# Ctrl+Shift+Delete → Clear all
```

---

## 🎓 Дополнительно

### Кастомный домен
1. Купите домен (Namecheap, Google Domains и т.д.)
2. Render Dashboard → Settings → Custom Domain
3. Добавьте CNAME записи как указано

### CI/CD pipeline
Render автоматически:
- Слушает push в main
- Запускает build
- Деплоит если успешно
- Откатывает если ошибка

### Откат версии
Render Dashboard → Deploys → выберите старую версию → **Redeploy**

---

## ✅ Чек-лист

- [ ] GitHub аккаунт создан ✓
- [ ] Код загружен на GitHub ✓
- [ ] Render аккаунт создан
- [ ] `render.yaml` добавлен в проект
- [ ] Web Service создан на Render
- [ ] Environment Variables установлены
- [ ] Deploy завершился успешно
- [ ] Фронтенд загружается
- [ ] API отвечает на `/health`
- [ ] NEXT_PUBLIC_API_URL обновлён

---

**🎉 Готово! Ваше приложение в интернете полностью бесплатно!**

---

## 📞 Поддержка

- **Render Docs**: https://render.com/docs
- **Discord**: https://discord.gg/render (сообщество)
- **Status**: https://status.render.com

Если есть проблемы - проверьте Logs в Render Dashboard!
