# 🔧 Быстрая установка FurniAI

## Шаг 1: Получить Google Gemini API ключ

1. Откройте https://ai.google.dev/
2. Нажмите кнопку "Get API Key"
3. Выберите "Create API key in new Google Cloud project" или используйте существующий проект
4. Скопируйте ваш API ключ (выглядит как `AIza...`)
5. **ВАЖНО:** Не делитесь этим ключом в публичных репозиториях!

## Шаг 2: Клонирование и установка

```bash
# Перейти в папку проекта
cd c:\furniture-ai-site

# Установить зависимости для всех workspace'ов
npm install

# Или отдельно:
cd frontend && npm install
cd ../backend && npm install
```

## Шаг 3: Конфигурация

### Backend (.env)
Создайте файл `backend/.env`:

```env
# Google Gemini API
GEMINI_API_KEY=AIza_YOUR_API_KEY_HERE

# Server
PORT=3001
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:3000
```

### Frontend (.env.local)
Создайте файл `frontend/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

## Шаг 4: Запуск

### В режиме разработки (оба сервера одновременно)
```bash
npm run dev
```

Откроется:
- 🌐 Frontend: http://localhost:3000
- 🔌 Backend: http://localhost:3001

### Backend отдельно
```bash
cd backend
npm run dev
```

### Frontend отдельно
```bash
cd frontend
npm run dev
```

## Шаг 5: Тестирование

Откройте http://localhost:3000 в браузере и:

1. Нажмите **"Загрузить интерьер"**
2. Выберите фото комнаты
3. Дождитесь анализа Gemini
4. Общайтесь с AI консультантом
5. Добавьте мебель
6. Опубликуйте интерьер

## 🆘 Решение проблем

### Backend не запускается

```bash
# Проверить Node.js версию (нужно 18+)
node --version

# Очистить кэш npm
npm cache clean --force

# Переустановить зависимости
cd backend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### "Cannot find module" ошибки

```bash
# Убедитесь что находитесь в папке backend
cd backend

# Переустановите зависимости
npm install

# Проверьте что .env существует
ls -la | grep .env
```

### Frontend показывает ошибку подключения

```bash
# Проверьте что backend запущен
curl http://localhost:3001/health
# Должна вернуться: {"status":"ok",...}

# Проверьте NEXT_PUBLIC_API_URL в браузере
# DevTools Console -> echo(process.env.NEXT_PUBLIC_API_URL)
```

### Ошибка при загрузке изображения

- Убедитесь что размер < 10MB
- Формат должен быть JPG, PNG, WebP, или GIF
- Проверьте права доступа на backend

### Gemini API возвращает ошибку

```bash
# Проверьте API ключ
cat backend/.env | grep GEMINI_API_KEY

# Ключ должен начинаться с "AIza"
# Проверьте что ключ полный (скопирован полностью)

# Проверьте лимиты на Google AI Studio:
# https://ai.google.dev/
```

## 📦 Примерная структура после установки

```
furniture-ai-site/
├── frontend/
│   ├── node_modules/
│   ├── .next/
│   ├── src/
│   ├── public/
│   ├── package.json
│   ├── next.config.js
│   └── .env.local
├── backend/
│   ├── node_modules/
│   ├── src/
│   │   ├── index.js
│   │   ├── routes/
│   │   └── services/
│   ├── data/
│   │   └── interiors.json (создаётся автоматически)
│   ├── package.json
│   └── .env
├── package.json
└── README.md
```

## 🎯 После установки

### Для разработки
```bash
# Запуск обоих серверов
npm run dev

# Логирование:
# - Frontend: http://localhost:3000
# - Backend API docs: curl http://localhost:3001/api/...
# - Console: F12 в браузере
```

### Для production
```bash
# Сборка
npm run build

# Запуск
npm start
```

## 🔒 Безопасность

⚠️ **ВАЖНО:** Никогда не коммитьте `.env` файлы!

```bash
# Убедитесь что .gitignore содержит:
*.env
*.env.local
.env.*.local
```

## 📚 Дополнительно

- **Gemini API документация:** https://ai.google.dev/docs
- **Next.js документация:** https://nextjs.org/docs
- **Express.js документация:** https://expressjs.com/

## ✅ Чек-лист установки

- [ ] Node.js 18+ установлен
- [ ] Google Gemini API ключ получен
- [ ] Переменные окружения (.env) установлены
- [ ] `npm install` выполнен
- [ ] `npm run dev` запущен без ошибок
- [ ] Frontend загружается на http://localhost:3000
- [ ] Backend отвечает на http://localhost:3001/health
- [ ] API ключ проверен в backend/.env

Готово! 🚀 Теперь вы можете начать использовать FurniAI!
