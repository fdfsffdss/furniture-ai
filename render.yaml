services:
  - type: web
    name: furniture-ai
    env: node
    plan: free
    buildCommand: bash build.sh
    startCommand: cd backend && node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
